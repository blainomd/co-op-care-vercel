# CareOS Monorepo

This is a monorepo. The actual codebase lives at:

```
careos-claude-code/careos-claude-code/
```

**Read the comprehensive CLAUDE.md there before doing any work:**

```
careos-claude-code/careos-claude-code/CLAUDE.md
```

That file contains the full project context: all routes, all database tables, billing engine rules, AI integration details, HIPAA requirements, environment variables, test infrastructure, and development workflow.

Do not create files at this root level. All code lives in `careos-claude-code/careos-claude-code/`.
